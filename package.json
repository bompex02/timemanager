{
  "name": "chroniq-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter backend --filter frontend run dev",
    "dev:all": "pnpm --parallel --filter @shared/types --filter backend --filter frontend run dev",
    "build:types": "pnpm --filter @shared/types run build"
  }
}